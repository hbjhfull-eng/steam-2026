# 전기 전도도로 읽는 중화 반응 — 센서 제작과 함수 모델링

고등학교 1학년 **통합과학 · 정보 · 수학 융합교육(STEAM)** 프로그램입니다.
직접 만든 마이크로비트 전기 전도도 센서로 중화점을 찾고, 그 데이터를 절댓값 함수로 표현합니다.
**학생 모집 페이지 + 50분 × 4차시 웹 활동지**로 구성됩니다.
(원 자료: KOFAC 융합교육 교재 「융합9. 디지털로 읽는 산과 염기의 중화 반응」)

* 정적 HTML 두 파일 — 빌드 과정 없음
* 외부 의존성은 Google Fonts CDN 하나뿐, 오프라인이면 시스템 글꼴로 대체
* 학생 입력은 **각자 기기의 브라우저(localStorage)** 에만 저장 — 서버·수집 없음
* 다크 모드 · 모바일 대응 · 인쇄(PDF) 최적화

---

## 미리보기

GitHub Pages 배포 후 주소:

```
https://<GitHub 사용자명>.github.io/conductivity-neutralization-steam/            → 모집 페이지
https://<GitHub 사용자명>.github.io/conductivity-neutralization-steam/activity.html → 학생 활동지
```

---

## 저장소 구조

```
conductivity-neutralization-steam/
├─ index.html        # 모집·소개 랜딩 페이지 (진입점)
├─ activity.html     # 학생 활동지 (4차시, localStorage 자동 저장)
├─ favicon.svg       # 브라우저 탭 아이콘
├─ .nojekyll         # GitHub Pages의 Jekyll 처리 비활성화 (정적 파일 그대로 서빙)
├─ .gitignore
├─ LICENSE           # CC BY-NC-SA 4.0
└─ README.md
```

> `.nojekyll` 은 빈 파일입니다. 없어도 대개 동작하지만, `_` 로 시작하는 파일이나
> 폴더를 두게 될 경우를 대비해 넣어 둡니다.

---

## 공개 전에 채워야 할 항목 (`index.html`)

`index.html` 상단 주석에 목록이 있고, 본문에서 <span> 주황색 점선 밑줄로 표시됩니다.
`〔  〕` 로 감싼 부분을 찾아 실제 정보로 바꾸세요.

| 표시 | 예시 |
|---|---|
| `〔학교명〕` | 한일여자고등학교 |
| `〔운영 요일·시간〕` | 매주 화요일 18:00–21:30 |
| `〔운영 기간〕` | 2026. 3.~6. (총 4주) |
| `〔정원〕` | 4명 (1개 반) |
| `〔신청 기간〕` | 2026. 3. 2.(월) ~ 3. 6.(금) 17:00 |
| `〔O월 중〕` | 2월 |
| `〔담당 교사〕` | 통합과학 ○○○ · 정보 ○○○ |
| `〔장소〕` | 창의융합실 1 |
| `〔문의〕` | 과학실 / 내선 000 |

### 신청 접수 (리로스쿨)

- 접수는 **리로스쿨에서 4개 과정을 함께 안내하며 일괄 진행**합니다. 이 페이지는 과정 안내용입니다.
- 기본 상태는 `id="apply"` 섹션의 **"신청 준비 중"** 배지입니다.
- 접수를 시작하면 그 섹션에서
  1. `<span class="cta-big disabled">신청 준비 중</span>` 과 바로 아래 `<p class="soon">…</p>` 삭제
  2. 그 아래 주석 처리된 `<a class="cta-big" href="리로스쿨_신청_페이지_URL">` 의 주석을 풀고
     `href` 에 리로스쿨 신청 페이지 주소 입력

---

## GitHub Pages 배포 방법

### 1. 저장소 만들기

GitHub에서 새 저장소를 만듭니다. 이름은 자유이며, 예시는 `conductivity-neutralization-steam` 입니다.
(이름을 바꾸면 위 미리보기 주소의 마지막 경로도 그 이름으로 바뀝니다.)

### 2. 파일 올리기

**A. 웹에서 드래그로 올리기 (가장 간단)**

1. 저장소 페이지 → **Add file → Upload files**
2. 이 폴더의 `index.html`, `favicon.svg`, `.nojekyll`, `LICENSE`, `README.md` 를 끌어다 놓기
   (`.nojekyll` 은 확장자가 없어 안 올라갈 때가 있으니, 안 되면 **Create new file** 로
   파일명에 `.nojekyll` 만 입력해 빈 파일로 커밋)
3. **Commit changes**

**B. git 명령으로 올리기**

```bash
cd conductivity-neutralization-steam
git init
git add .
git commit -m "전기 전도도로 읽는 중화 반응 — 모집 페이지 + 4차시 활동지"
git branch -M main
git remote add origin https://github.com/<사용자명>/conductivity-neutralization-steam.git
git push -u origin main
```

### 3. Pages 켜기

1. 저장소 **Settings → Pages**
2. **Build and deployment → Source** 를 **Deploy from a branch** 로
3. **Branch** 를 `main` / `/ (root)` 로 지정하고 **Save**
4. 1\~2분 뒤 상단에 게시 주소가 표시됩니다.

### 4. 업데이트

파일을 고쳐 다시 커밋/푸시(또는 웹에서 다시 업로드)하면 1분 내외로 자동 반영됩니다.

---

## 모집 단계 사용법

1. `index.html` 의 `〔  〕` 항목을 채우고 커밋 (신청은 리로스쿨에서 4개 과정 일괄 접수)
2. 배포 주소(`…/conductivity-neutralization-steam/`)를 QR·가정통신문·리로스쿨 공지에 함께 안내
3. 학생은 페이지에서 과정을 확인하고 **리로스쿨**에서 희망 과정을 선택해 지원
4. 리로스쿨 접수가 열리면 위 "신청 접수" 안내대로 `id="apply"` 섹션을 링크 버튼으로 교체

## 수업 단계 사용법 (활동지)

1. `…/conductivity-neutralization-steam/activity.html` 주소를 공유
2. 학생은 **각자 기기**에서 접속 → 상단에 학번·이름·모둠 입력
3. 차시별(1\~4 버튼)로 이동하며 빈칸·표·체크리스트 작성 (입력 자동 저장, 진행률 표시)
4. 제출은 **[답안 복사]** (정리된 텍스트가 클립보드로) 또는 **[인쇄 · PDF]**

> localStorage 저장이므로 **다른 기기·시크릿창·브라우저 데이터 삭제 시 내용이 사라집니다.**
> 차시 사이에 기기가 바뀌면 그때그때 [답안 복사]로 보관하도록 안내하세요.

---

## 수업 구성 (4차시)

| 차시 | 주제 | 교과 | 학생 핵심 활동 |
|---|---|---|---|
| 1 | 산과 염기의 중화 반응 | 통합과학 | 성질표 · 알짜 이온 반응식 · **자바실험실** 이온 수 변화표 |
| 2 | 전기 전도도 측정기 만들기 | 정보 · 공학 | **마이크로비트** 배선 · 측정 알고리즘 설계 · MakeCode 코딩 |
| 3 | 중화 반응 실험 & 데이터 수집 | 통합과학 · 데이터 | 회차별 전도도 측정 · 스프레드시트 그래프 · V자 해석 |
| 4 | 지오지브라로 함수 모델링 | 통합과학 · 공통수학 | 절댓값 함수 `y=a\|x−p\|+q` · 예측 · 중화열 함수 확장 |

성취기준: `[10통과2-01-03]` `[10통과2-01-04]` `[12정03-07]`

---

## 커스터마이즈

* **모집 정보**: `index.html` 의 `〔  〕` 표시 + 리로스쿨 신청 페이지 주소(접수 시작 시)
* **색상 / 글꼴**: 두 파일 모두 상단 `<style>` 안 `:root` 의 CSS 변수 (동일 팔레트)
* **참고(예시) 전도도 데이터**: `activity.html` 3차시 표의 `참고값` 행(`class="fixed"` 셀), 현재 지도서 예시값 `15 13 11 9 7 5 3 1 3 5 7 9`
* **1회 첨가량·실험 조건**: `activity.html` 3차시 활동 1 문구
* **활동지 저장 키 초기화**: 브라우저 콘솔에서 `localStorage.removeItem('ddr-jungwha-v1')`

---

## 출처 및 라이선스

* 원 자료: 한국과학창의재단(KOFAC) 2022 개정 교육과정 통합과학·정보 융합교육(STEAM)
  고등학교 1학년 2학기 교재·지도서 「융합9. 디지털로 읽는 산과 염기의 중화 반응」
* 이 활동지: 위 자료를 4차시 수업용으로 재구성한 2차 저작물
* 라이선스: [CC BY-NC-SA 4.0](LICENSE) — 출처 표시 · 비영리 · 동일조건 변경 허락
